server:
  port: 8082

spring:
  application:
    name: wizard-service
  datasource:
    url: jdbc:postgresql://localhost:5432/wizarddb
    username: postgres
    password: 122432
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
